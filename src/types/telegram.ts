export interface TelegramUser {
  id: number;
  first_name: string;
  last_name?: string;
  username?: string;
  photo_url?: string;
  auth_date: number;
  hash: string;
}

// Remove the duplicate Window declaration here, as it's already defined in telegram-webapp.d.ts
